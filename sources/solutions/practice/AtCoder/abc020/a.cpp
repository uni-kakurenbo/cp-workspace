#include<bits/stdc++.h>
using namespace std;

using ll = long long;

#define REP(i,n) for(int i=0; i<int(n); i++)
#define FOR(i,a,b) for(int i=a; i<=int(b); i++)
#define FORA(i,I) for(auto& i:I)

#define ALL(x) x.begin(),x.end()

int main() {
    int a; cin >> a;
    cout << (a == 1?"ABC":"chokudai") << endl;
    return 0;
}
