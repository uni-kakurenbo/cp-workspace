#include<bits/stdc++.h>
using namespace std;
typedef long long ll;
 
signed main(){
  string s;
  cin >> s;
  if(s.substr(s.size() - 2, 2) == "er") cout << "er";
  else cout << "ist";
  cout << endl;
}
